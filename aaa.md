after job finishes it should send an api request (with timestamp and dataset hash) to server saying you should reaload
server turns a global var true so that the file contentr gets replaced


dataset identifier=file name and timestamp

cm job deploy